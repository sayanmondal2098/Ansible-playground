---
- hosts: all
  tasks:
    - name: install packages
      become_user: root
      ansible_password: '{{ lookup("env", "ANSIPASS") }}'
      become: true
      apt:
        state: present
        name:
         - htop
